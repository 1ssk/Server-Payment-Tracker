services:
  app:
    build: .
    ports:
      - "8080:8080"
    environment:
      - DB_PATH=/data/app.db
      - ADMIN_USERNAME=
      - ADMIN_PASSWORD=
      - AUTH_TOKEN=
    volumes:
      - db-data:/data

volumes:
  db-data:

